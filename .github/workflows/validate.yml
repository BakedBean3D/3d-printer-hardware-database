name: validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install pyyaml jsonschema

      - name: Validate database
        run: python3 scripts/validate.py

      - name: Generated aggregates are in sync
        run: |
          python3 controller_boards/gen.py
          python3 psu/gen.py
          git diff --exit-code

      - name: Generated JSON Schemas are in sync
        run: |
          python3 scripts/gen_schema.py
          git diff --exit-code

      - name: Data validates against generated schemas
        run: python3 scripts/gen_schema.py --validate

      - name: submit-hardware.sh syntax
        run: bash -n scripts/submit-hardware.sh

      - name: Docs and category routing in sync with validate.py
        run: python3 scripts/check_docs.py
